name: Keep Render Alive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    name: Ping Render App
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: |
          echo "=== Calling health endpoint ==="
          curl -v https://betrelloclone.onrender.com/health
